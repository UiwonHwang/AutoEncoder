# pre-processing
axis : 0
test_size : 1000

# model architecture
ae_shape : [115, 80]

# optimizer parameters
decay : 0.95
num_decay_steps : 150
init_learning_rate : 0.005 # if minibatch=True, we recommend 0.005 as initial learning rate. else, 0.03

# training parameters
epochs : 1000
batch_size : 10
log_every : 10 # write log every N step
hidden_size : 4
anim_path : '1D_GAN.mp4'
minibatch_layer : True
